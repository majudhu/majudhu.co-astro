---
interface Props {
    title: string;
    slug: string;
}
const { title, slug } = Astro.props as Props;
---

<a
    href={`#${slug}`}
    class="block text-center w-full p-3 my-8 bg-gray-300 font-semibold nohover hover:font-bold hover:bg-white active:"
>
    {title}
</a>
